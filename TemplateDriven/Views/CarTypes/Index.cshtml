@model IEnumerable<TemplateDriven.Models.CarType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarType_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarType_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CarType_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.CarType_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CarType_Id })
        </td>
    </tr>
}

</table>
